import javax.swing.*;

public class EventPanel extends JPanel {
    //event: Event   // the event that this panel displays.
    Event event;

    //completeButton: JButton
    JButton completeButton;

    // a button that completes the Event (sets the complete variable to true).  Note, this should only appear on events that implement Completable.
    //EventPanel should display the data of the Event: name, time, name, duration (if applicable), location (if applicable), and completion status.
    //You can decide whether to drawStrings in the paintComponent function or to add labels and have them display.

}
